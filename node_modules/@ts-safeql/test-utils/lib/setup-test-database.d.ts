import postgres from "postgres";
interface ConnectionOptions {
    host: string;
    port: number;
    user: string;
    password: string;
    database: string;
}
export declare function mapConnectionOptionsToString(connectionOptions: ConnectionOptions): string;
export declare function setupTestDatabase(params: {
    databaseName: string;
    postgresUrl: string;
}): Promise<{
    drop: () => Promise<number>;
    sql: postgres.Sql<{}>;
    databaseUrl: string;
    databaseName: string;
}>;
export declare function generateTestDatabaseName(): string;
export {};
