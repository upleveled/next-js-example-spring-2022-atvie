import { DatabaseInitializationError } from "@ts-safeql/shared";
import { taskEither } from "fp-ts";
export interface ConnectionOptions {
    host: string;
    port: number;
    user: string;
    password: string;
    database: string;
}
export declare function mapConnectionOptionsToString(connectionOptions: ConnectionOptions): string;
export declare function parseConnection(databaseUrl: string): ConnectionOptions;
export declare function initDatabase(connection: ConnectionOptions): taskEither.TaskEither<DatabaseInitializationError, void>;
export declare function createDatabase(connection: ConnectionOptions): taskEither.TaskEither<DatabaseInitializationError, void>;
export declare function dropDatabase(connection: ConnectionOptions): taskEither.TaskEither<DatabaseInitializationError, void>;
