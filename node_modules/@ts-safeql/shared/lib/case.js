"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toScreamingSnakeCase = exports.toSnakeCase = exports.toPascalCase = exports.toCamelCase = exports.toCase = void 0;
const common_1 = require("./common");
function toCase(str, caseType) {
    switch (caseType) {
        case undefined:
            return str;
        case "snake":
            return toSnakeCase(str);
        case "camel":
            return toCamelCase(str);
        case "pascal":
            return toPascalCase(str);
        case "screaming snake":
            return toScreamingSnakeCase(str);
        default:
            (0, common_1.assertNever)(caseType);
    }
}
exports.toCase = toCase;
function toCamelCase(str) {
    return str.replace(/([-_][a-z])/gi, ($1) => {
        return $1.toUpperCase().replace("-", "").replace("_", "");
    });
}
exports.toCamelCase = toCamelCase;
function toPascalCase(str) {
    return toCamelCase(str).replace(/^[a-z]/, (val) => val.toUpperCase());
}
exports.toPascalCase = toPascalCase;
function toSnakeCase(str) {
    return str
        .replace(/([a-z])([A-Z])/g, "$1_$2")
        .replace(/([A-Z])([A-Z][a-z])/g, "$1_$2")
        .toLowerCase();
}
exports.toSnakeCase = toSnakeCase;
function toScreamingSnakeCase(str) {
    return toSnakeCase(str).toUpperCase();
}
exports.toScreamingSnakeCase = toScreamingSnakeCase;
//# sourceMappingURL=case.js.map